# Gate G3 — stakeholder dress rehearsals — PASSED 2026-08-03

Plan: ralplan-clockchain-bilateral-handshake-v2.md (steps 19–21).
Release: `18c4e8950f769a34a2fccf762a902c45cffb1378` (branch `handshake-codex`).

## Dress rehearsal evidence

| Run | Payer | Requestor | Outcome |
|---|---|---|---|
| `g2-run-3` | operator-hosted child | fresh Hermes agent (`handshake_requester`), one pasted prompt, 6m15s | AUTHORIZED (blocks 2706962 / 2706983 / 2706985) |
| `g3-dr1` | **fresh Hermes agent** (`handshake_payer`), one pasted prompt + payer handoff dir, 4m58s | fresh Hermes agent (`handshake_requester`), one pasted prompt, 4m33s | AUTHORIZED (blocks 2710483 / 2710504 / 2710526) |
| `g2-kr` | operator-hosted | CLI, relay kill -9 mid-session | AUTHORIZED after restarts |
| `g2-kr3` | operator-hosted | CLI, requestor kill -9 in anchor window | AUTHORIZED after restart |

Replay-safety: every run journaled (batch A four fresh keys, batch B the
announced requestor plus three fresh reserves, all nonce-0); no address
funded twice across all runs; the byte-pinned record and the
FUNDING_REPLAYED negative check hold.

## M3 deliverables

- `prompts/payer.md` + `prompts/requestor.md` — both proven by fresh
  agents with zero coaching in `g3-dr1`.
- `docs/runbook.md` — one page, quotes the two-regime budget verbatim.
- `runs/{id}.json` — immutable, append-only: g2-run-3, g2-kr, g2-kr3,
  g3-dr1.
- Rehearsal checklist: covered by the runbook's operator steps and the
  failure-posture section (kill/restart matrix above).

## Agent boundary compliance (g3-dr1, both agents)

- Neither agent printed keys, manifests, tokens, or evidence contents.
- Neither agent funded addresses or moved payment; `paymentMoved:false`
  on every status line.
- Both agents declined to claim authorization; only the operator's
  fresh aggregate verifier published the verdict.

## Monitor (G1b evidence for this gate)

`scripts/render-monitor.mjs` rendered both views for session
`9893090e-…`: phase `verified`, 8 mapped messages, verdict published,
three anchor receipts. Pre-verdict renders provably never contain the
final-verdict word (test/monitor.test.mjs).
